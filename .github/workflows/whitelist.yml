name: Whitelist Bastion Users

on:
  push:
    branches: [ main ]
    paths: [ whitelist.yaml ]
  pull_request:
    branches: [ main ]
    paths: [ whitelist.yaml ]


jobs:
  setup:
    name: Setup Run
    runs-on: ubuntu-latest
    run: |
      echo "Hi"

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2
        with:
          persist-credentials: "false"
      - name: Determine Environment
        run: |
          echo "Hello"